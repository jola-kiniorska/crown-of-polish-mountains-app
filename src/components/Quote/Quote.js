import styles from './Quote.module.scss';

const Quote = () => {
    return(
        <div>
            <p className={styles.quote}>A journey of a thousand miles begins with a single step</p>
        </div>
    );
};

export default Quote;